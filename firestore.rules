rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regra global que permite acesso completo.
    // ATENÇÃO: Isto não é seguro para produção.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
